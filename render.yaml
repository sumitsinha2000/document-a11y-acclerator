services:
  - type: web
    name: doca11y-backend
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libqpdf-dev
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
