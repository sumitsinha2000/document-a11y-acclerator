[pytest]
pythonpath = .
addopts = -m "not alt_fallback"
markers =
    slow_pdf: tests that run the full PDF analyzer on real PDFs
    alt_fallback: exercises the AU fallback heuristic for MCID-less figures
